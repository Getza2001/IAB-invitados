import 'package:flutter/material.dart';

import '../styles/colors/colors_view.dart';

class InfoMesa extends StatelessWidget {
  const InfoMesa({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
        //va vista con numero de mesa
        );
  }
}
