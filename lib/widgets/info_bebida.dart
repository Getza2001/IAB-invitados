import 'package:flutter/material.dart';

import '../styles/colors/colors_view.dart';

class CheckboxBebida extends StatelessWidget {
  const CheckboxBebida({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
        //va vistas de checkbox
        );
  }
}
